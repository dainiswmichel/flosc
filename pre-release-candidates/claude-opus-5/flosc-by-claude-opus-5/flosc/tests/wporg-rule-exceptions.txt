# Reviewed exceptions to tests/check_wporg_rules.php
#
# Format:  path:line:RULE-ID:reason
#
# A line with no written reason is counted as a FINDING, not an exception, and
# the active count prints on every run. An exceptions file is how a gate quietly
# stops testing, so each entry has to be argued in writing and stays visible.

includes/filesystem/class-flosc-filesystem.php:179:WPORG-02:file.php is loaded under `! function_exists( 'WP_Filesystem' )` so the filesystem API exists around the WP_Filesystem_Direct fallback four lines below. WordPress.org reviewed this exact tree on 13 Sep 2026 and flagged only the import.php include, not this one. Removing it risks the direct-write path used when the global filesystem is not ready.
includes/filesystem/class-flosc-filesystem.php:253:WPORG-02:Same shape as :179 in the sibling method. Same reasoning, same review evidence.
includes/filesystem/flosc-data-paths.php:382:WPORG-02:Same shape again: file.php loaded to bring up the filesystem API before the WP_Filesystem_Direct fallback. Not flagged by WordPress.org on 13 Sep 2026.
